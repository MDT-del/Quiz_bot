# wsgi.py
# این فایل فقط باید آبجکت 'app' را برای Gunicorn فراهم کند.
# تمام کارهای اولیه (مانند ایجاد جداول و راه‌اندازی ترد ربات)
# و همچنین تنظیمات لاگینگ پایه، توسط gunicorn.conf.py (هوک when_ready) انجام می‌شود.

from admin_panel import app

# اگر نیاز به لاگ خاصی در لحظه ایمپورت wsgi.py دارید، می‌توانید اضافه کنید،
# اما معمولاً نیازی نیست چون gunicorn.conf.py لاگینگ را مدیریت می‌کند.
# import logging
# logger = logging.getLogger(__name__)
# logger.info("wsgi.py: آبجکت 'app' آماده برای Gunicorn.")

# Gunicorn این فایل را ایمپورت می‌کند و به دنبال متغیری به نام app می‌گردد.
# هیچ کد دیگری برای اجرا در سطح بالای این ماژول لازم نیست.
